"use strict";

const sum = (a, b) => {
  return a + b;
};

const brokenSum = (a) => {
  return a + b;
};

const minus = (a, b) => {
  return a - b;
};

const brokeMinus = (b) => {
  return b;
};

const multiply = (a, b) => {
  return a * b;
};

console.log(sum(2, 2)); // returns 4
console.log(sum(2, 5)); // returns 7
console.log(sum("a", "b")); // returns ab
